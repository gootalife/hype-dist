{
  "name": "hype-dist",
  "version": "1.0.0",
  "main": "hypeDist.js",
  "repository": "https://github.com/gootalife/hype-dist.git",
  "author": "moso",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.1",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.+(ts|tsx)",
      "**/?(*.)+(spec|test).+(ts|tsx)"
    ]
  }
}
